/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['localhost', 'api.example.com', 'ghjlgiozqywusdlipjrt.supabase.co'],
  },
};

export default nextConfig;
